name: Docker Image
on:
  pull_request:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FIFTH_P: plead
      MONGO_PROD_DB: ${{ secrets.MONGO_PROD_DB}}
    concurrency:
      group: docker_image
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v3
      - name: Uses Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: 'npm'

      - run: npm install
      - run: npm run build
      -  run:
          echo "THE DB IS $MONGO_PROD_DB"
          echo "THE p IS $FIFTH_P"
      - name: Build and publish Docker Image
        uses: mr-smithers-excellent/docker-build-push@v5
        with:
          image: ${{ secrets.DOCKER_HUB_USERNAME }}/atlp-pulse-bn
          tags: latest
          registry: docker.io
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
      - name: Login to DO and push a new image
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USERNAME }}
          key: ${{ secrets.DO_KEY }}
          port: ${{ secrets.DO_PORT }}
          script: |
            docker pull devpulse/atlp-pulse-bn:latest
            docker ps | grep 4000 | cut -f1 -d' ' | xargs docker stop
            docker run -p 4000:4000 -d -e REGISTER_FRONTEND_URL=https://beta.devpulse.org/register -e REGISTER_ORG_FRONTEND_URL=https://beta.devpulse.org/signup/org -e FRONTEND_LINK=https://beta.devpulse.org -e  MONGO_PROD_DB="mongodb+srv://doadmin:e617MB4IVm5rC092@pulse-prod-db-70e37aa6.mongo.ondigitalocean.com/devpulse-backend-staging?tls=true&authSource=admin&replicaSet=pulse-prod-db" devpulse/atlp-pulse-bn:latest